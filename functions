#Разбить строку на слова
#String to words
def words(str):
    i, j, w = 0, 0, []
    
    for i in range(len(text) + 1):
        try:
            if text[i] != ' ':
                continue
            else:
                words.append(text[j:i])
                j = i + 1
        except IndexError:
            words.append(text[j:])
